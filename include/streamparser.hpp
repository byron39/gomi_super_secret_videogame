#pragma once

#include <raylib.h>
#include "types.hpp"

/*
    parse an embedded array of bytes that is a dump of the ressource file into a workable object
 */

Image ParseEmbeddedImage(byte* stream){
    
}